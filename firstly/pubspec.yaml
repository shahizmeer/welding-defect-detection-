name: firstly
description: Welding Defect Detection App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ✅ Compatible Firebase packages
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.15.8
  firebase_storage: ^11.6.9
  firebase_app_check: ^0.2.2+7  # ✅ FIXED

  # ✅ Other packages
  image_picker: ^1.0.7
  http: ^1.2.1
  fl_chart: ^1.0.0
  intl: ^0.18.1
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
